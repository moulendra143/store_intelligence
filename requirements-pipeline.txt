# ── Computer Vision / YOLOv8 Detection Pipeline ──────────────────────
# ⚠️  WARNING: This installs PyTorch + torchvision (~2.5 GB).
# Only install this if you are running pipeline/run.py against video clips.
# Install with: pip install -r requirements-pipeline.txt
# Docker target: pipeline

opencv-python-headless
ultralytics
numpy
pandas
shapely
requests
python-dotenv
